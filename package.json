{
  "name": "root",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "release": "standard-version",
    "semantic-release": "npx semantic-release"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lerna": "^7.3.0"
  }
}
