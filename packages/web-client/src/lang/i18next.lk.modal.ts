export type LkButton = {
  button: {
    save: string;
    ok: string;
    cancel: string;
    submit: string;
    add: string;
    reset: string;
  };
};

export type LkApp = {
  app: {
    welcome: string;
    message: string;
  };
};
