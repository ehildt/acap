import { Meta, Story, Canvas } from '@storybook/blocks';
import Rotate from './Rotate.stories';

<Meta title="MDX/Rotate" of={Rotate} />

# Rotate

With `MDX` we can include a story for `Rotate` right in the middle of our
markdown documentation.

<Canvas>
  <Story of={Rotate.RotateSamples} />
</Canvas>

